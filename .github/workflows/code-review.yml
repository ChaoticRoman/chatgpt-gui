name: Code Review

on:
  pull_request:
    branches: [ main ]

jobs:
  code-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: ChaoticRoman/gpt-code-review-action@v11
        with:
          openai-key: ${{ secrets.OPENAI_API_KEY }}
          max-length: 50000
          model: gpt-5.3-codex
